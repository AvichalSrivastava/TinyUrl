{
  "name": "tinyurl",
  "version": "1.0.0",
  "description": "system design 1st project",
  "license": "ISC",
  "type": "commonjs",
  "main": "dist/handler.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "esbuild src/handler/handler.ts --bundle --platform=node --target=node20 --outfile=dist/handler.js",
    "zip": "zip -r lambda.zip dist",
    "dev": "ts-node --transpile-only src/local.ts",
    "clean": "npx rimraf dist",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1000.0",
    "@aws-sdk/lib-dynamodb": "^3.1000.0",
    "dotenv": "^17.3.1",
    "ioredis": "^5.10.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/ioredis": "^4.28.10",
    "@types/node": "^25.3.5",
    "esbuild": "^0.27.3",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
